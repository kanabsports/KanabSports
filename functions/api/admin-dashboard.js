const OWNER_EMAIL='howdy@kanabsports.com';

export async function onRequestGet({request,env}){
  if(!env.SPORTS_DB)return json({authenticated:false},503);
  const session=await authenticate(request,env.SPORTS_DB);if(!session)return json({authenticated:false},401);
  await dataSchema(env.SPORTS_DB);
  const [access,documents,submissions,recent]=await Promise.all([
    env.SPORTS_DB.prepare(`SELECT id,name,email,organization,sport,role,created_at FROM coach_access_requests WHERE status='pending' ORDER BY created_at DESC LIMIT 20`).all(),
    env.SPORTS_DB.prepare(`SELECT id,name,email,team,sport,document_type,season,filename,created_at FROM coach_documents WHERE status='pending' ORDER BY created_at DESC LIMIT 20`).all(),
    env.SPORTS_DB.prepare(`SELECT id,type,name,email,team,sport,event_date,opponent,result,created_at FROM coach_submissions WHERE status='pending' ORDER BY created_at DESC LIMIT 20`).all(),
    env.SPORTS_DB.prepare(`SELECT type,team,sport,status,COALESCE(reviewed_at,published_at,created_at) AS activity_at FROM coach_submissions WHERE status!='pending' UNION ALL SELECT document_type AS type,team,sport,status,COALESCE(reviewed_at,created_at) AS activity_at FROM coach_documents WHERE status!='pending' ORDER BY activity_at DESC LIMIT 8`).all()
  ]);
  const items=[...(access.results||[]).map(x=>({...x,kind:'coach_access',title:`Coach Access · ${x.name} · ${x.sport}`,detail:`${x.organization} · ${x.role}`})),...(documents.results||[]).map(x=>({...x,kind:'document',title:`${x.document_type} · ${x.team} · ${x.sport}`,detail:`${x.season} · ${x.filename}`})),...(submissions.results||[]).map(x=>({...x,kind:'submission',title:`${x.type} · ${x.team||x.sport||x.name}`,detail:[x.sport,x.event_date,x.opponent,x.result].filter(Boolean).join(' · ')}))].sort((a,b)=>String(b.created_at).localeCompare(String(a.created_at)));
  const [verified,last,adminUser,coachRows,teamRows,registrationRows]=await Promise.all([
    env.SPORTS_DB.prepare(`SELECT COUNT(*) AS count FROM coach_access_requests WHERE status='approved'`).first(),
    env.SPORTS_DB.prepare(`SELECT MAX(COALESCE(published_at,reviewed_at,created_at)) AS at FROM coach_submissions WHERE status='approved'`).first(),
    env.SPORTS_DB.prepare(`SELECT email FROM admin_users WHERE email=? LIMIT 1`).bind(OWNER_EMAIL).first(),
    env.SPORTS_DB.prepare(`SELECT id,name,email,organization,sport,role,status,created_at,reviewed_at FROM coach_access_requests ORDER BY created_at DESC LIMIT 100`).all(),
    env.SPORTS_DB.prepare(`SELECT team,sport,MAX(activity_at) AS activity_at FROM (SELECT team,sport,created_at AS activity_at FROM coach_documents WHERE team!='' UNION ALL SELECT team,sport,created_at AS activity_at FROM coach_submissions WHERE team IS NOT NULL AND team!='') GROUP BY team,sport ORDER BY activity_at DESC LIMIT 100`).all(),
    env.SPORTS_DB.prepare(`SELECT id,title,sport,ages,price,deadline,season_dates,status,registration_url,updated_at FROM registrations ORDER BY updated_at DESC`).all()
  ]);
  return json({authenticated:true,user:{email:session.email,role:'Owner',hasPassword:Boolean(adminUser)},stats:{pending:items.length,verifiedCoaches:Number(verified?.count||0),updatesNeeded:0,lastPublished:last?.at||null},items,recent:recent.results||[],coaches:coachRows.results||[],teams:teamRows.results||[],registrations:registrationRows.results||[],health:{database:true,email:Boolean(env.RESEND_API_KEY),site:true}});
}

export async function onRequestPost({request,env}){
  if(!env.SPORTS_DB)return json({success:false,error:'Database unavailable.'},503);const session=await authenticate(request,env.SPORTS_DB);if(!session)return json({success:false,error:'Your admin session has expired.'},401);
  try{const body=await request.json(),kind=clean(body.kind,40),id=clean(body.id,100),action=clean(body.action,30);await dataSchema(env.SPORTS_DB);
    if(action==='set_password'){
      const password=String(body.password||'');if(password.length<12)return json({success:false,error:'Use at least 12 characters.'},400);if(password.length>200)return json({success:false,error:'That password is too long.'},400);
      const saltBytes=crypto.getRandomValues(new Uint8Array(16)),salt=Array.from(saltBytes,b=>b.toString(16).padStart(2,'0')).join(''),iterations=50000,hash=await passwordHash(password,salt,iterations);
      await env.SPORTS_DB.prepare(`INSERT INTO admin_users (email,password_hash,password_salt,password_iterations,updated_at) VALUES (?,?,?,?,datetime('now')) ON CONFLICT(email) DO UPDATE SET password_hash=excluded.password_hash,password_salt=excluded.password_salt,password_iterations=excluded.password_iterations,updated_at=datetime('now')`).bind(OWNER_EMAIL,hash,salt,iterations).run();return json({success:true});
    }
    if(action==='save_registration'){
      const title=clean(body.title,160),sport=clean(body.sport,100),ages=clean(body.ages,100),price=clean(body.price,80),deadline=clean(body.deadline,40),seasonDates=clean(body.season_dates,160),status=clean(body.status,40),url=cleanUrl(body.registration_url);if(!title||!sport||!status)return json({success:false,error:'Title, sport, and status are required.'},400);
      const registrationId=id||crypto.randomUUID();await env.SPORTS_DB.prepare(`INSERT INTO registrations (id,title,sport,ages,price,deadline,season_dates,status,registration_url,updated_at,created_at) VALUES (?,?,?,?,?,?,?,?,?,datetime('now'),datetime('now')) ON CONFLICT(id) DO UPDATE SET title=excluded.title,sport=excluded.sport,ages=excluded.ages,price=excluded.price,deadline=excluded.deadline,season_dates=excluded.season_dates,status=excluded.status,registration_url=excluded.registration_url,updated_at=datetime('now')`).bind(registrationId,title,sport,ages,price,deadline,seasonDates,status,url).run();return json({success:true,id:registrationId});
    }
    if(action==='delete_registration'){if(!id)return json({success:false,error:'Registration not found.'},400);await env.SPORTS_DB.prepare(`DELETE FROM registrations WHERE id=?`).bind(id).run();return json({success:true})}
    if(!id||!['approve','reject'].includes(action)||!['coach_access','document','submission'].includes(kind))return json({success:false,error:'Invalid action.'},400);
    if(kind==='coach_access'){
      const row=await env.SPORTS_DB.prepare(`SELECT id,name,email,status FROM coach_access_requests WHERE id=? LIMIT 1`).bind(id).first();if(!row||row.status!=='pending')return json({success:false,error:'This request is no longer pending.'},409);
      if(action==='approve'){
        if(!env.RESEND_API_KEY||!env.COACH_ACCESS_CODE)return json({success:false,error:'Coach password email is not configured.'},503);const origin=new URL(request.url).origin,upload=`${origin}/coach-documents.html`;
        const sent=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${env.RESEND_API_KEY}`,'Content-Type':'application/json','Idempotency-Key':`admin-coach-approve-${id}`},body:JSON.stringify({from:'Kanab Sports <website@kanabsports.com>',to:[row.email],subject:'Your Kanab Sports coach access is approved',text:`Hi ${row.name},\n\nUpload page: ${upload}\nCoach password: ${env.COACH_ACCESS_CODE}`,html:`<div style="font-family:Arial,sans-serif;line-height:1.6"><h2>Your coach access is approved</h2><p>Hi ${esc(row.name)},</p><p><a href="${esc(upload)}">Open coach upload</a></p><p><strong>Coach password:</strong> ${esc(env.COACH_ACCESS_CODE)}</p></div>`})});if(!sent.ok)return json({success:false,error:'The password email could not be sent. Nothing was approved.'},502);
      }
      await env.SPORTS_DB.prepare(`UPDATE coach_access_requests SET status=?,reviewed_at=datetime('now'),review_token_hash=NULL WHERE id=? AND status='pending'`).bind(action==='approve'?'approved':'rejected',id).run();
    }else if(kind==='document')await env.SPORTS_DB.prepare(`UPDATE coach_documents SET status=?,reviewed_at=datetime('now'),review_token_hash=NULL WHERE id=? AND status='pending'`).bind(action==='approve'?'approved':'rejected',id).run();
    else await env.SPORTS_DB.prepare(`UPDATE coach_submissions SET status=?,reviewed_at=datetime('now'),published_at=?,review_token_hash=NULL WHERE id=? AND status='pending'`).bind(action==='approve'?'approved':'rejected',action==='approve'?new Date().toISOString():null,id).run();
    return json({success:true});
  }catch(error){console.error('admin action error',error);return json({success:false,error:'The action could not be completed.'},500)}
}

async function authenticate(request,db){await authSchema(db);const cookie=request.headers.get('Cookie')||'',match=cookie.match(/(?:^|;\s*)ks_admin=([^;]+)/);if(!match)return null;const hash=await sha256(decodeURIComponent(match[1]));return db.prepare(`SELECT email FROM admin_sessions WHERE token_hash=? AND email=? AND datetime(expires_at)>datetime('now') LIMIT 1`).bind(hash,OWNER_EMAIL).first()}
async function authSchema(db){await db.prepare(`CREATE TABLE IF NOT EXISTS admin_sessions (id TEXT PRIMARY KEY,email TEXT NOT NULL,token_hash TEXT NOT NULL UNIQUE,expires_at TEXT NOT NULL,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)`).run();await db.prepare(`CREATE TABLE IF NOT EXISTS admin_users (email TEXT PRIMARY KEY,password_hash TEXT NOT NULL,password_salt TEXT NOT NULL,password_iterations INTEGER NOT NULL,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)`).run()}
async function dataSchema(db){await db.prepare(`CREATE TABLE IF NOT EXISTS coach_access_requests (id TEXT PRIMARY KEY,name TEXT NOT NULL,email TEXT NOT NULL,organization TEXT NOT NULL,sport TEXT NOT NULL,role TEXT NOT NULL,phone TEXT,team_url TEXT,message TEXT,status TEXT NOT NULL DEFAULT 'pending',review_token_hash TEXT,review_expires_at TEXT,reviewed_at TEXT,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)`).run();await db.prepare(`CREATE TABLE IF NOT EXISTS coach_documents (id TEXT PRIMARY KEY,verification_id TEXT NOT NULL,name TEXT NOT NULL,email TEXT NOT NULL,sport TEXT NOT NULL,team TEXT NOT NULL,document_type TEXT NOT NULL,season TEXT NOT NULL,notes TEXT,filename TEXT NOT NULL,byte_size INTEGER NOT NULL,status TEXT NOT NULL DEFAULT 'pending',review_token_hash TEXT,review_expires_at TEXT,is_test INTEGER NOT NULL DEFAULT 0,test_expires_at TEXT,reviewed_at TEXT,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)`).run();await db.prepare(`CREATE TABLE IF NOT EXISTS coach_submissions (id TEXT PRIMARY KEY,source TEXT NOT NULL,type TEXT NOT NULL,name TEXT,email TEXT,team TEXT,sport TEXT,event_date TEXT,opponent TEXT,result TEXT,link TEXT,message TEXT,status TEXT NOT NULL DEFAULT 'pending',review_token_hash TEXT,review_expires_at TEXT,reviewed_at TEXT,published_at TEXT,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)`).run();await db.prepare(`CREATE TABLE IF NOT EXISTS registrations (id TEXT PRIMARY KEY,title TEXT NOT NULL,sport TEXT NOT NULL,ages TEXT,price TEXT,deadline TEXT,season_dates TEXT,status TEXT NOT NULL,registration_url TEXT,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)`).run()}
async function sha256(v){const h=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(v));return Array.from(new Uint8Array(h),b=>b.toString(16).padStart(2,'0')).join('')}
async function passwordHash(password,saltHex,iterations){const key=await crypto.subtle.importKey('raw',new TextEncoder().encode(password),'PBKDF2',false,['deriveBits']),salt=new Uint8Array(saltHex.match(/.{2}/g).map(x=>parseInt(x,16))),bits=await crypto.subtle.deriveBits({name:'PBKDF2',hash:'SHA-256',salt,iterations},key,256);return Array.from(new Uint8Array(bits),b=>b.toString(16).padStart(2,'0')).join('')}
function clean(v,max=500){return String(v||'').replace(/[\u0000-\u001F\u007F]/g,' ').trim().slice(0,max)}
function cleanUrl(v){const raw=clean(v,800);if(!raw)return'';try{const u=new URL(raw);return u.protocol==='https:'?u.href:''}catch{return''}}
function esc(v){return String(v).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;')}
function json(data,status=200){return new Response(JSON.stringify(data),{status,headers:{'Content-Type':'application/json; charset=utf-8','Cache-Control':'no-store'}})}
